<% if @comments.present? %>
	<% @comments.each do |comment|%>
		<%= comment.commenter %><br>
		<%= comment.body %><br>
		<%= comment.created_at %><br>
	<% end %>
 <% else %>
	Sorry No Comment on this acticle
 <% end %></br>
<%= link_to "view articles", articles_path%>